# Datasets and Hydra working dirs
data/
results/

# Regenerated figure PDFs; the figure scripts produce these from paper_results/.
figures/outputs/

# Paper PDF kept on disk only (not redistributed via the repo).
Paper.pdf

# Local-only result/plot archives. paper_results/ holds the canonical paper
# data; for now we don't publish it (decide later: LFS / Zenodo).
paper_results/
plots/
models/

# Historical artifacts kept on disk for safekeeping but not pushed.
old_results/
old_plots/
cineca_results/

# Python caches and editor noise
__pycache__/
*.py[co]
.ipynb_checkpoints/
.DS_Store
